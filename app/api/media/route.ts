import {env} from 'cloudflare:workers';import {owner,db} from '@/lib/menucraft/server';
export async function POST(request:Request){try{if(request.headers.get('origin')!==new URL(request.url).origin)return new Response('Forbidden',{status:403});const r=await owner();if(Number(request.headers.get('content-length'))>6*1024*1024)throw new Error('Image too large');const form=await request.formData(),file=form.get('file');if(!(file instanceof File)||file.size>5*1024*1024||!['image/jpeg','image/png','image/webp'].includes(file.type))throw new Error('Invalid image');const bytes=new Uint8Array(await file.arrayBuffer());const valid=file.type==='image/webp'?String.fromCharCode(...bytes.slice(0,4))==='RIFF'&&String.fromCharCode(...bytes.slice(8,12))==='WEBP':file.type==='image/png'?bytes[0]===137&&bytes[1]===80&&bytes[2]===78&&bytes[3]===71:bytes[0]===255&&bytes[1]===216&&bytes[2]===255;if(!valid)throw new Error('Invalid image contents');const id=crypto.randomUUID();await env.FILES.put(id,bytes,{httpMetadata:{contentType:file.type}});await db().prepare('INSERT INTO media(id,restaurant_id,name,mime) VALUES(?,?,?,?)').bind(id,r.id,file.name.slice(0,200),file.type).run();return Response.json({id})}catch(e){return Response.json({error:String(e)},{status:400})}}
