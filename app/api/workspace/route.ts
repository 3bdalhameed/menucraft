import {workspace,save,owner,db} from '@/lib/menucraft/server';import {stateSchema} from '@/lib/menucraft/model';
export async function GET(){try{return Response.json(await workspace())}catch(e){return Response.json({error:String(e)},{status:401})}}
export async function POST(request:Request){try{if(request.headers.get('origin')!==new URL(request.url).origin)return Response.json({error:'Invalid origin'},{status:403});const raw=await request.text();if(raw.length>2000000)return Response.json({error:'Workspace too large'},{status:413});const body=JSON.parse(raw);const actor=await owner();const recent=await db().prepare('SELECT COUNT(*) AS count FROM events WHERE menu_id=? AND kind=? AND created_at>?').bind(actor.id,'write',new Date(Date.now()-60000).toISOString()).first<{count:number}>();if((recent?.count||0)>120)return Response.json({error:'Too many requests. Try again shortly.'},{status:429});await db().prepare('INSERT INTO events(id,menu_id,kind,created_at) VALUES(?,?,?,?)').bind(crypto.randomUUID(),actor.id,'write',new Date().toISOString()).run();if(body.action==='save'){const state=stateSchema.parse(body.state);return Response.json({revision:await save(state)})}const r=await owner();if(body.action==='publish'){const row=await db().prepare('SELECT data FROM records WHERE restaurant_id=? AND id=? AND kind=?').bind(r.id,body.menuId,'menu').first<{data:string}>();if(!row)throw new Error('Menu not found');const menu=JSON.parse(row.data),snapshot=JSON.stringify({profile:JSON.parse(r.profile),menu}),date=new Date().toISOString();await db().batch([db().prepare('INSERT INTO publications(id,restaurant_id,slug,snapshot,published_at) VALUES(?,?,?,?,?) ON CONFLICT(id) DO UPDATE SET snapshot=excluded.snapshot,published_at=excluded.published_at WHERE publications.restaurant_id=excluded.restaurant_id').bind(menu.id,r.id,menu.slug,snapshot,date),db().prepare('INSERT INTO versions(id,restaurant_id,menu_id,snapshot,created_at) VALUES(?,?,?,?,?)').bind(crypto.randomUUID(),r.id,menu.id,snapshot,date)]);return Response.json({ok:true})}if(body.action==='restore'){const v=await db().prepare('SELECT snapshot FROM versions WHERE id=? AND restaurant_id=?').bind(body.versionId,r.id).first<{snapshot:string}>();if(!v)throw new Error('Version not found');return Response.json(JSON.parse(v.snapshot));}throw new Error('Unknown action')}catch(e){return Response.json({error:e instanceof Error?e.message:'Request failed'},{status:400})}}
