import {sqliteTable,text,integer,index} from 'drizzle-orm/sqlite-core';
export const restaurants=sqliteTable('restaurants',{id:text('id').primaryKey(),owner:text('owner').notNull().unique(),slug:text('slug').notNull().unique(),profile:text('profile').notNull(),revision:integer('revision').notNull().default(0),writeToken:text('write_token').notNull().default('')});
export const records=sqliteTable('records',{id:text('id').primaryKey(),restaurantId:text('restaurant_id').notNull().references(()=>restaurants.id),kind:text('kind').notNull(),data:text('data').notNull()},t=>[index('records_restaurant_kind').on(t.restaurantId,t.kind)]);
export const publications=sqliteTable('publications',{id:text('id').primaryKey(),restaurantId:text('restaurant_id').notNull().references(()=>restaurants.id),slug:text('slug').notNull(),snapshot:text('snapshot').notNull(),publishedAt:text('published_at').notNull()},t=>[index('publications_restaurant_slug').on(t.restaurantId,t.slug)]);
export const versions=sqliteTable('versions',{id:text('id').primaryKey(),restaurantId:text('restaurant_id').notNull().references(()=>restaurants.id),menuId:text('menu_id').notNull(),snapshot:text('snapshot').notNull(),createdAt:text('created_at').notNull()},t=>[index('versions_restaurant_menu').on(t.restaurantId,t.menuId)]);
export const media=sqliteTable('media',{id:text('id').primaryKey(),restaurantId:text('restaurant_id').notNull().references(()=>restaurants.id),name:text('name').notNull(),mime:text('mime').notNull()},t=>[index('media_restaurant').on(t.restaurantId)]);
export const events=sqliteTable('events',{id:text('id').primaryKey(),menuId:text('menu_id').notNull(),kind:text('kind').notNull(),createdAt:text('created_at').notNull()},t=>[index('events_menu').on(t.menuId)]);
